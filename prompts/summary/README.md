# Summary prompt

A single non-Lens summary call per ReviewRun. Same Prefix as the Lenses; produces a Run-level summary persisted on `review_runs`, not as a Finding.

File: `summary_v1.tmpl`. Same naming conventions as `prompts/lens/`.
